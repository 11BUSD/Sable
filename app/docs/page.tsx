import { Nav } from "@/components/Nav";
export default function Docs(){return <><Nav/><main className="docs"><span className="badge">Specification 0.1</span><h1>VEIL threat model.</h1><p>VEIL is an experimental privacy transport interface for Ethereum. The prototype focuses on metadata minimization around RPC access and creates deterministic receipts that make the transport policy inspectable.</p><h2>What we are trying to protect</h2><p>The design aims to reduce unnecessary linkage between an origin network identity, an application session, an operator identity, and an Ethereum RPC request. Ethereum consensus data that must be public is not magically hidden.</p><h2>Adversary model</h2><p>The MVP assumes relays do not all collude. A global passive adversary, browser compromise, malicious endpoint, timing correlation and production-grade traffic analysis are <strong>not</strong> solved by this demo.</p><h2>Ring authorization</h2><p>The architecture reserves a provider interface for anonymous-set authorization. The public tester intentionally does <strong>not</strong> ship an unaudited home-grown ring-signature implementation. A production release should integrate a reviewed cryptographic construction and independent audit.</p><h2>Receipt format</h2><pre>{`{\n  "version": "veil-receipt/0.1",\n  "intentHash": "sha256(...) ",\n  "policyHash": "sha256(...) ",\n  "routeCommitment": "sha256(...) ",\n  "relayCount": 3,\n  "security": "prototype-not-audited"\n}`}</pre><h2>Tester success criteria</h2><p>A tester should be able to understand the difference between direct RPC and the VEIL route in under one minute, run a read-only request without an account, and export a receipt suitable for attaching to a bug report.</p></main></>}
