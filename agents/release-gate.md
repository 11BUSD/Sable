# Release Gate Agent

Release only when: build passes; health endpoint passes; public RPC methods are allow-listed; no secrets are committed; threat model matches behavior; prototype warnings are visible; tester flow completes on mobile width; receipt exports deterministically shaped JSON.
